# JavaScript Rules

Deepsource collection of static analysis rules and anti-patterns for the JavaScript language.

Each Markdown file in this directory corresponds to a specific rule (e.g., `JS-0001.md`) and contains:
- A description of the issue (severity, type)
- An example of problematic code (`Bad Practice`)
- The recommended solution (`Recommended`)

*Data extracted from DeepSource.*